diff --git a/node_modules/svg-captcha/lib/option-manager.js b/node_modules/svg-captcha/lib/option-manager.js
index 6a75c40..b44c4dd 100644
--- a/node_modules/svg-captcha/lib/option-manager.js
+++ b/node_modules/svg-captcha/lib/option-manager.js
@@ -4,7 +4,13 @@ const opentype = require('opentype.js');
 const charPreset = require('./char-preset');
 
 const fontPath = path.join(__dirname, '../fonts/Comismsh.ttf');
-const font = opentype.loadSync(fontPath);
+let font;
+try {
+	font = opentype.loadSync(fontPath);
+} catch (e) {
+	console.warn('Failed to load default font:', e.message);
+	font = { unitsPerEm: 1000, ascender: 800, descender: -200, forDummy: true }; // Dummy font object
+}
 const ascender = font.ascender;
 const descender = font.descender;
 
